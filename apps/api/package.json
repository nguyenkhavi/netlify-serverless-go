{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "env-cmd ts-node-dev --respawn --exit-child --project tsconfig.json -r tsconfig-paths/register --transpile-only src/main.fastify.ts",
    "lint": "eslint src/*.ts",
    "start": "pnpm run dev"
  },
  "engines": {
    "node": ">=16"
  }
}





