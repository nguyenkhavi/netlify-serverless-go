{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --exit-child --project tsconfig.json -r tsconfig-paths/register --transpile-only src/main.fastify.ts",
    "lint": "eslint src/*.ts",
    "start": "pnpm run dev"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
