{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --exit-child --project tsconfig.json -r tsconfig-paths/register --transpile-only src/main.ts",
    "lint": "eslint src/*.ts",
    "start": "pnpm run dev"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "@fastify/cors": "^8.2.1",
    "cors": "^2.8.5",
    "fastify": "^4.15.0",
    "ts-node-dev": "^2.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13"
  }
}
