{
  "name": "rpc",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate": "drizzle-kit generate:mysql",
    "migrate": "env-cmd -f ../../apps/landing/.env drizzle-kit push:mysql"
  }
}
